-- run the below sql commands locally (won't be run in web app) --
create database forumapi;
use forumapi;
create table test (id int unsigned, msg varchar(200));
insert into test values(111,'nice, hello!');
